# Production (Vercel serverless / WSGI) dependencies.
# The ASGI/Channels stack (channels, daphne, twisted, ...) is intentionally
# NOT listed here: Vercel runs this app under WSGI and excludes Channels from
# INSTALLED_APPS, and that stack adds ~50MB that pushes the function toward the
# 250MB unzipped serverless limit. For local ASGI development install the full
# set with:  pip install -r requirements-dev.txt
Django>=5.0,<5.3
dj-database-url>=2.0
psycopg[binary]>=3.1
whitenoise>=6.0
websockets>=12.0           # lightweight client used by the analysis services
pandas>=2.2
python-dotenv>=1.0
requests>=2.31             # AI chat panel (Claude API) + Forex Factory calendar feed
aiohttp>=3.9               # Async HTTP for news sentiment
feedparser>=6.0            # RSS feed parsing for news
django-allauth>=0.60.0     # Google OAuth authentication
pytz>=2023.3               # Timezone support for market hours
numpy>=1.24.0              # Numerical operations for ML strategies
